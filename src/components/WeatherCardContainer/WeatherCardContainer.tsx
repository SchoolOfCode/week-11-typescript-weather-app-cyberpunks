import React from 'react'
import WeatherCard from '../WeatherCard/WeatherCard'

type Props = {}

const WeatherCardContainer = (props: Props) => {
  return (
    <div>
        WeatherCardContainer
    <WeatherCard/>
    </div>
  )
}

export default WeatherCardContainer